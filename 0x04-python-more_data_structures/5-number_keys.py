#!/usr/bin/python3

def number_keys(a_dictionary):
    """get number of keys in dictionary

    Args:
        a_dictionary (dictionary): dictionary

    Returns:
        number: number of keys in dictionary
    """
    return len(a_dictionary)
